// this file can be moved to /src/module_with with the `mod.rs` name (as /src/module_in_folder/mod.rs), but nowadays is prefered to have modules in the src folder and submodules in folders with the same name

pub mod submodule;

// pub is necessary to make it public and accesible from main.rs